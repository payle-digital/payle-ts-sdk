declare module 'payle' {
    export namespace Payle {
        enum PaymentMethod {
            CREDIT_CARD = 'credit_card',
            BOLETO = 'boleto',
            PIX = 'pix',
            DEBIT_CARD = 'debit_card',
        }
    }
}